const dark = "#000022";
const light = "#FBF5F3";
const accent = "#F87060";

export const lightTheme = {
  dark: dark,
  light: light,
  bgColor: light,
  color: dark,
  accent: accent,
};

export const darkTheme = {
  dark: dark,
  light: light,
  bgColor: dark,
  color: light,
  accent: accent,
};
